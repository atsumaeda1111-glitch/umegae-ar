# umegae-ar

「梅ヶ枝 現象学的ブーストAR」— 単一ファイルのWebAR実験プロトタイプ。

## 構成

全実装は `index.html` 1ファイル（約1,180行）に収まっている。ビルド・依存管理・テストなし。
three.js r128 のみ CDN 読み込み（156行目）。

**全文を読む必要はない。以下の行範囲で必要な箇所だけを開くこと。**

| 範囲 | 内容 |
|---|---|
| 7-155 | `<style>` |
| 158-184 | body DOM（HUD / エラーパネル / ログパネル / デバッグパネル） |
| 196-228 | 実験条件 `CONDITION` とグローバル状態 |
| 230-367 | 実験ログ `ExperimentLog`（送信 328 / CSV出力 355） |
| 369-553 | カメラ・センサー起動とフォールバック |
| 555-842 | Web Audio グラフ（剥離音・足音・風・梵鐘・残響） |
| 844-970 | three.js 描画（背景透過・層別ビジュアル） |
| 972-1013 | ジャイロによる静止判定 |
| 1015-1119 | 「剥がす」操作と伊予街道ナラティブの同期 |
| 1121-1176 | 起動シーケンス |

## 動作の要点

- **実験条件**: URLに `?cond=A|B|C`。既定は `C`（本システム＝引き算のAR）。
- **デバッグ**: URLに `?debug=1` で内部状態を画面下に表示。
- **進行**: `layerIndex` 0（未開始）→ 1..4（剥がした枚数）。4枚目で全音停止。
- **起動は2タップ**: iOSのセンサー許可と音声アンロックのため、1タップ目で
  `AudioContext` を起動する。ユーザー操作ハンドラの外でオーディオを初期化しないこと。

## 触る際の注意

- **デプロイ先は Google Apps Script のWebアプリ**で、iframe内で動作する。
  397行目の `<?!= ScriptApp.getService().getUrl() ?>` は**GASがデプロイ時に評価する
  スクリプトレット**。壊さないこと。外部ホスティング時は評価されないため、
  398-400行目の固定URLにフォールバックする。
- iframe自身のURL（`userCodeAppPanel`）はリクエストごとの一時URLのため、
  直接開くと白画面になる。`openSelfInNewTab()` はこのために存在する。
- 実機のiOS Safariでしか再現しない挙動（センサー許可・音声アンロック・`100dvh`）が
  多い。動作確認できない変更は、その旨を明示すること。
- コミットメッセージは日本語で、`v1.5:` のように版数を先頭に付ける慣例。
